#!/usr/bin/env bash
set -euo pipefail

if command -v gitleaks >/dev/null 2>&1; then
    exec gitleaks git --pre-commit --staged --redact
else
    exec nix run --quiet nixpkgs#gitleaks -- git --pre-commit --staged --redact
fi
